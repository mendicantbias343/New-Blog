---
interface Props {
  date: string;
  display: boolean;
}

const { date, display } = Astro.props;
const jsDate = new Date(date);
import dayjs from "dayjs";
---

<time datetime={jsDate.toISOString()}>
  {display && dayjs(date).format("MMM D, YYYY")}
</time>
